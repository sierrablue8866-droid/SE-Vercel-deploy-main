# ─── Security-sensitive files — NEVER commit these ──────────────────────────
# SSL certificates
*.crt
*.ca-bundle
*.p7b
*.key
*.pem
ssl/
certs/

# Twilio / API recovery codes
*recovery_code*
*twilio_2fa*

# Firebase service accounts
service-account.json
*-firebase-adminsdk-*.json
*sa-key*.json
google-credentials.json
*.patch
*.zip

# Environment files
.env
.env.local
.env.production
*.env
!.env.example

# Cloned Sub-repositories
ECC/
Client page/
qm/
python-docs-samples/
python-docs-samples-gcp/
.archive/

# Leaked / one-off credential helpers — never commit auth helper scripts
askpass.bat
askpass.sh

# ─── Dependencies ───────────────────────────────────────────────────────────
libs/
node_modules/
.pnp
.pnp.js
.venv/
venv/

# ─── Build outputs ───────────────────────────────────────────────────────────
dist/
build/
.next/
/out/
.nuxt/
.cache/
.turbo/
.vercel/
.firebase/
coverage/
*.tsbuildinfo
next-env.d.ts
apps/admin-dashboard/api/server.js
apps/admin-dashboard/api/server.js.map

# ─── WhatsApp session / cache data — contains live auth tokens ─────────────
.wwebjs_auth/
whatsapp-auth/
n8n-data/

# ─── Legacy / archived / unused snapshots ───────────────────────────────────
_unused_archive/
_archived_repos/
.vercel-old/
temp3

# ─── AI tooling scaffolding — not part of the deployable app ───────────────
.agent/
.agents/
.claude/
.codex/
.replit
.replitignore

# ─── IDE ──────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ─── OS ───────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ─── Secrets ────────────────────────────────────────────────────────────────
secrets/
!package.json
!tsconfig.json

# ─── Logs ─────────────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
eslint-results.json
*.apk
.codebuddy
graphify-out/
.eve/
.output/

# ─── Additional coverage from merged upstream ignore rules ──────────────────
.env*.local
service-account*.json
firebase-adminsdk*.json
.vercel
.env*

wa_sessions/
packages/whatsapp-agent/*.sw_col.json

# CodeBridge for OneStream
# OneStream local metadata store (user-specific, regenerated via Import)
.onestream/
# OneStream connection configurations (contains sensitive data)
.onestream-metadata/connections.json
# VS Code extension packages
*.vsix
# OneStream file selections (user-specific)
.onestream-selections.json
# OneStream authentication tokens
onestream-token-*
# C# build artifacts
obj/
bin/
**/obj/
**/bin/
