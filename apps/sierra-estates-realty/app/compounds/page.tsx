import '../client/houzez.css';
import CompoundsPortal from '../client/CompoundsPortal';

export const metadata = {
  title: 'Sierra Estates · Compounds',
  description: 'New Cairo compound intelligence — 29 compounds benchmarked by AI score, growth and price.',
};

export default function CompoundsPage() {
  return <CompoundsPortal />;
}
