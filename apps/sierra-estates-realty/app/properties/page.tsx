import '../client/houzez.css';
import PropertiesPortal from '../client/PropertiesPortal';

export const metadata = {
  title: 'Sierra Estates · Properties',
  description: 'AI-curated rent & resale inventory across New Cairo — every unit verified on-site.',
};

export default function PropertiesPage() {
  return <PropertiesPortal />;
}
