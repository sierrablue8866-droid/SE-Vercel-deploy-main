# Deployment Policy

This file is intentionally a pointer to the canonical deployment policy in the repo root:

- Root source of truth: `../DEPLOYMENT.md`
- This `docs/` copy exists only to avoid broken relative links from older references.
- Do not duplicate architecture or command lists here; keep the contract in the root file only.

The root deployment file is the only authoritative version for:
- app architecture
- backend ownership
- command list
- deployment matrix
- worker workflow policy
